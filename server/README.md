Pour utiliser pour l'instant
lancer le code serveur
depuis un nouveau terminal
nc localhost 8080
ensuite 3 commandes dispo
LOGIN:name:password -> se connecter au serveur
LIST -> recuperer la liste de tout les messages (automatiser cette commande à chaque message envoyé peu importe le client qui l'envoie chaque client doit executer cette commande pour rester à jour)
MSG:contenu -> ecrire un message dans le chat + enregistrer dans la db le message
